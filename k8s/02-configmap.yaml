apiVersion: v1
kind: ConfigMap
metadata:
  name: library-config
  namespace: library-system
data:
  NODE_ENV: "production"
  PORT: "5000"
  JWT_EXPIRES_IN: "7d"
  CLIENT_URL: "*"          # tighten to your real domain/ingress host in production
  FINE_PER_DAY: "5"
  LOAN_PERIOD_DAYS: "14"
